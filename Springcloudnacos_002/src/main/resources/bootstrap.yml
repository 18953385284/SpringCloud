spring:
  application:
    name: CloudNaCos02 # 服务名称
  profiles:
    active: dev # 开发环境，这里是dev
  cloud:
    nacos:
      server-addr: localhost:8847 # Nacos地址
      config:
        file-extension: yml # 文件后缀名
